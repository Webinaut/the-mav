---
import BlogPostLayout from "../layouts/BlogPostLayout.astro";
import ViewCounter from "../components/ViewCounter.astro";
import PostComments from "../components/PostComments.astro";
import Btn from "../components/Button.astro";

import { getEntry, render } from "astro:content";

const { slug } = Astro.params;
if (slug === undefined) {
  throw new Error("Slug is required");
}

const entry = await getEntry("articles", slug);
if (entry === undefined) {
  return Astro.redirect("/404");
}

const { Content } = await render(entry);
---

<BlogPostLayout frontmatter={entry.data}>
  <article class="article-content">
    <ViewCounter slug={slug} />

    {/* Wrap your content area in a designated container */}
    <div id="article-markdown-body" class="markdown-body">
      <Content />
    </div>

    <Btn buttonlink="/articles/" buttontext="Back To Articles" />
  </article>

  <article>
    <h3 style="text-align: center;">Comments</h3>
    <p>
      The comments section is a place for healthy, constructive and challenging
      conversations. The basic rule is simple – I encourage you to share your
      views, but be respectful of the author, yourself and your fellow
      commenters. To comment, register or sign in to GitHub (it's free). This
      protects us from spam.
    </p>
    <PostComments />
  </article>
</BlogPostLayout>

{/* POST-RENDER PATH RESOLVER SCRIPT */}
<script is:inline>
  (function () {
    const container = document.getElementById("article-markdown-body");
    if (!container) return;

    // Scan the content block looking for any embedded image elements
    const images = container.getElementsByTagName("img");

    for (let img of images) {
      const src = img.getAttribute("src");

      // If the image tag contains our target relative prefix string
      if (src && src.startsWith("./")) {
        const cleanFilename = src.replace("./", "");

        // Find the current window path location context (e.g., /articles/my-post/)
        let baseRoute = window.location.pathname;
        if (!baseRoute.endsWith("/")) baseRoute += "/";

        // Direct the browser to resolve the image from the compiled post folder asset bundle
        img.setAttribute("src", baseRoute + cleanFilename);
      }
    }
  })();
</script>

<style is:global>
  /* Global Float Layout Styles */
  .article-content img.cms-floated-image {
    height: auto;
    display: inline-block;
    border-radius: 6px;
    max-width: 100%;
  }

  /* --- ALIGNMENT CLASSES --- */
  .article-content img.align-left {
    float: left;
  }
  .article-content img.align-right {
    float: right;
  }
  .article-content img.align-center {
    display: block;
    float: none;
    margin-left: auto;
    margin-right: auto;
  }

  /* --- WIDTH LAYOUT CLASSES --- */
  .article-content img.size-small {
    width: 30% !important;
  }
  .article-content img.size-medium {
    width: 45% !important;
  }
  .article-content img.size-large {
    width: 60% !important;
  }
  .article-content img.size-full {
    width: 100% !important;
    display: block;
    margin-top: 2rem;
    margin-bottom: 2rem;
  }

  /* --- MARGIN CONFIGURATION CLASSES --- */
  .article-content img.align-left.margin-normal {
    margin: 0.5rem 1.5rem 1rem 0 !important;
  }
  .article-content img.align-left.margin-wide {
    margin: 1.5rem 2.5rem 2rem 0 !important;
  }

  .article-content img.align-right.margin-normal {
    margin: 0.5rem 0 1rem 1.5rem !important;
  }
  .article-content img.align-right.margin-wide {
    margin: 1.5rem 0 2rem 2.5rem !important;
  }

  .article-content::after {
    content: "";
    display: table;
    clear: both;
  }
</style>
