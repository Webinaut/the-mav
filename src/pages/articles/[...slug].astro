---
import BlogPostLayout from "../../layouts/BlogPostLayout.astro";
import ViewCounter from "../../components/ViewCounter.astro";
import PostComments from "../../components/PostComments.astro";
import Btn from "../../components/Button.astro";

import { getEntry, render } from "astro:content";

const { slug } = Astro.params;
if (slug === undefined) {
  throw new Error("Slug is required");
}

const entry = await getEntry("articles", slug);
if (entry === undefined) {
  return Astro.redirect("/404");
}

// Fixed: Single argument rendering prevents Astro 5 server-side template crashes
const { Content } = await render(entry);
---

<BlogPostLayout frontmatter={entry.data}>
  <article class="article-content">
    <ViewCounter slug={slug} />

    {/* Bound container for our client script to process */}
    <div id="production-markdown-content" class="markdown-body">
      <Content />
    </div>

    <Btn buttonlink="/articles/" buttontext="Back To Articles" />
  </article>

  <article>
    <h3 style="text-align: center;">Comments</h3>
    <p>
      The comments section is a place for healthy, constructive and challenging
      conversations. The basic rule is simple – I encourage you to share your
      views, but be respectful of the author, yourself and your fellow
      commenters. To comment, register or sign in to GitHub (it's free). This
      protects us from spam.
    </p>
    <PostComments />
  </article>
</BlogPostLayout>

{/* STABLE PRODUCTION PATH RESOLVER */}
<script is:inline>
  (function () {
    function processImages() {
      const contentArea = document.getElementById(
        "production-markdown-content",
      );
      if (!contentArea) return;

      const images = contentArea.getElementsByTagName("img");
      const isLocalhost =
        window.location.hostname === "localhost" ||
        window.location.hostname === "127.0.0.1";

      for (let i = 0; i < images.length; i++) {
        const img = images[i];
        let rawConfigString = "";
        let liveProductionUrl = "";

        // 1. Capture Astro's live optimized asset route string from the srcset payload
        const rawSrcset = img.getAttribute("srcset") || "";
        if (rawSrcset) {
          // Extracts the first compiled URL path string out of the asset compilation grid array
          liveProductionUrl = rawSrcset.split(",")[0].trim().split(" ")[0];
        }

        // 2. Intercept Astro 5's internal metadata tracking object to extract your style hashes
        if (img.hasAttribute("__astro_image_")) {
          try {
            const astroData = JSON.parse(img.getAttribute("__astro_image_"));
            if (astroData && astroData.src) {
              rawConfigString = astroData.src;
            }
          } catch (e) {
            console.error(
              "Failed parsing internal __astro_image_ metadata block:",
              e,
            );
          }
        }

        // Fallback to reading standard src parameters if metadata trackers are blank
        if (!rawConfigString) {
          rawConfigString = img.getAttribute("src") || "";
        }

        // 3. Process your custom configuration options if your Sveltia hash is present
        if (rawConfigString && rawConfigString.includes("#")) {
          const parts = rawConfigString.split("#");
          const hashConfig = parts[1];

          if (hashConfig) {
            const styles = hashConfig.split("_");
            const align = styles[0] || "right";
            const size = styles[1] || "size-medium";
            const margin = styles[2] || "margin-normal";

            // 4. Resolve the image source dynamically based on your active host environment
            if (isLocalhost) {
              // Local Development: Build directory path pointing to our Vite middleware helper
              let currentPath = window.location.pathname;
              if (!currentPath.endsWith("/")) currentPath += "/";
              const fileFragment = parts[0].split("/").pop();
              img.setAttribute("src", currentPath + fileFragment);
            } else {
              // Live Production (Netlify): Map directly to Astro's real compiled production link
              // If the srcset array is blank, fallback safely to the native source reference root
              const finalLivePath = liveProductionUrl || img.src || "";
              if (finalLivePath.includes("#")) {
                img.setAttribute("src", finalLivePath.split("#")[0]);
              } else {
                img.setAttribute("src", finalLivePath);
              }
            }

            // 5. Remove temporary attributes to prevent browser layout conflicts
            img.removeAttribute("srcset");
            img.removeAttribute("__astro_image_");

            // 6. Inject your custom styles as clean CSS classes for Flexbox layout control
            img.className = "";
            img.classList.add("cms-floated-image");
            img.classList.add("align-" + align);
            img.classList.add(size);
            img.classList.add(margin);
          }
        }
      }
    }

    // Run layout calculation rules immediately upon DOM tree initialization
    processImages();

    // Attach Mutation Observer safety net to handle complex asynchronous content updates
    const targetNode = document.getElementById("production-markdown-content");
    if (targetNode) {
      const observer = new MutationObserver(function () {
        processImages();
      });
      observer.observe(targetNode, { childList: true, subtree: true });
    }
  })();
</script>

<style is:global>
  /* --- MODERN FLEXBOX PARAGRAPH STRUCTURE --- */
  .article-content p:has(img.cms-floated-image) {
    display: flex;
    flex-direction: row;
    align-items: flex-start;
    margin-bottom: 2rem;
    width: 100%;
  }

  /* Base Image Styles */
  .article-content img.cms-floated-image {
    height: auto;
    border-radius: 6px;
    flex-shrink: 0;
  }

  /* --- ALIGNMENT CLASSES VIA FLEX ORDER --- */
  .article-content img.align-left {
    order: -1;
  }
  .article-content img.align-right {
    order: 1;
  }
  .article-content p:has(img.align-center) {
    flex-direction: column;
    align-items: center;
  }
  .article-content img.align-center {
    order: 0;
  }

  /* --- WIDTH LAYOUT CLASSES --- */
  .article-content img.size-small {
    width: 30% !important;
  }
  .article-content img.size-medium {
    width: 45% !important;
  }
  .article-content img.size-large {
    width: 60% !important;
  }
  .article-content p:has(img.size-full) {
    flex-direction: column;
    align-items: stretch;
  }
  .article-content img.size-full {
    width: 100% !important;
  }

  /* Map spacing parameters directly to modern layout gaps */
  .article-content p:has(img.margin-normal) {
    gap: 1.5rem;
  }
  .article-content p:has(img.margin-wide) {
    gap: 2.5rem;
  }

  /* Keep blockquotes beautifully isolated from overlapping margins */
  .article-content blockquote {
    margin: 2rem 0;
    padding: 1rem 1.5rem;
    border-left: 4px solid #ccc;
    width: 100%;
    box-sizing: border-box;
  }
</style>
